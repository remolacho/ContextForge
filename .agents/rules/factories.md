# Factories Rules
- Implementar `ProviderFactory` y `LLMFactory`.
- Utilizar registro dinámico para permitir la adición de nuevos tipos (proveedores o motores LLM) sin modificar el código base de la factoría.
- Cumplir estrictamente con el principio Open/Closed.
