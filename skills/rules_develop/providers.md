# Providers Rules
- Implementar la `ProviderInterface`.
- Cada proveedor (ej. `YouTrackProvider`) es responsable de transformar el JSON específico del API en campos genéricos para el `ContextItemBuilder`.
- Asegurar la compatibilidad con el factor de registro dinámico.
